channels:
  - conda-forge
  - bioconda
dependencies:
  # min version dependencies based on the scanpy recipe
  - python >=3.9
  - python-igraph >=0.10
  - leidenalg >=0.10.2
  - matplotlib-base >=3.6
  - natsort
  - networkx >=2.7
  - numpy >=1.23
  - scanpy >=1.10
  - scikit-learn >=1.1
  - tqdm

  # dev
  - black
  - isort
  - pytest
  - ruff
